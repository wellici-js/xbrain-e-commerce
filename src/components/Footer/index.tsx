import React, { FC } from 'react';

import Form from '../Form';

const Footer: FC = () => {
  return (
    <footer>
      <h3>Dados do Cliente</h3>
      <Form />
    </footer>
  );
}

export default Footer;
